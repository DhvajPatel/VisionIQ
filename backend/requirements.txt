fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

pillow>=10.4.0
numpy>=2.3

# CPU-only torch — avoids downloading 1.5 GB of CUDA libs on Render's servers.
# ~250 MB vs ~1.7 GB for the CUDA build. Render has no GPU anyway.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

transformers>=4.45,<5.0.0

# facenet-pytorch: installed without its stale Pillow constraint.
# The build command in render.yaml runs two pip install steps to handle this.

# HuggingFace gender models (auto-downloaded on first request, not at build time):
#   1. rizvandwiki/gender-classification    (ViT-B/16, ~330 MB)
#   2. rizvandwiki/gender-classification-2  (ViT-L/16, ~1.2 GB)
#   3. prithivMLmods/Gender-Classifier-Mini (SigLIP2,  ~900 MB)
